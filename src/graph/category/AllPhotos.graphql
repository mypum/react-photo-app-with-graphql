query allPhotosQuery($category: String) {
  photos(category: $category) {
    id,
    name,
    rating,
    timesViewed,
    category,
    imageUrl {
      medium
    }
    user {
      id,
      fullname,
      userPicUrl
    }
  }
}
